class Solution {
public:
  typedef long long ll;
  long long countPalindromePaths(vector<int>& parent, string s) {
    int n = parent.size();
    unordered_map<ll, ll>cnt;
    vector<vector<pair<int, char>>>tr(n + 1);
    for()
  }
};